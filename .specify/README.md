# Spec-Kit Notes

`specify init --here --ai codex --force --no-git --ignore-agent-tools` was attempted for this project, but the local `specify` command failed because it could not find a matching release asset for `codex`.

This folder preserves the expected project-level spec home so future spec-kit commands can be re-run once the local template issue is fixed.

