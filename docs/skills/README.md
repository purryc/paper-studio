# Skill Contracts

This folder records the local skill contracts Paper Studio expects.

It intentionally does not contain full copies of installed skills from `~/.agents/skills` because those folders can contain private paths, helper scripts, credentials expectations, and user-specific policy. The repo keeps only the stable interface needed to reproduce the Paper Studio pipeline.

